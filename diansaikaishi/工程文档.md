# 工程文档

> 当前基线：2026-07-26，源码基于提交 `22a6cae`（本次文档更新前）。
>
> 本文描述当前工作区的真实实现和已知边界；状态结论来自源码、提交记录与 `P6收尾与新版底层全面迁移总体方案.md`。
>
> P6 文档是 2026-07-24 的迁移设计与验收基线，不再代表 2026-07-26 的全部实现状态。若本文、P6 方案和源码冲突，以当前源码及实车结果为准。

## 1. 当前结论

这是一个 TI CCS / MSPM0G3507 小车工程。当前主线已经从旧开环底层切换为：

```text
任务动作
→ CarController 统一仲裁
→ 循迹 / 航向 / 转角 / 定距控制
→ 左右 normalized 命令（-1000～1000）
→ MotorControl 双轮轮速闭环
→ Motor 硬件 PWM + 方向 GPIO
→ TB6612 + 电机
→ 编码器反馈
```

当前最重要的状态是：

| 模块 | 当前状态 | 说明 |
|---|---|---|
| P2 硬件 PWM | 已启用，已有实车验收记录 | TIMG8、20 kHz，软件 PWM 只保留为编译回退 |
| P3 编码器、轮速、里程 | 已启用，已有实车验收记录 | ENCA 双边沿 ×2 解码，20 ms 估算轮速与累计距离 |
| P4 双轮轮速闭环 | 已启用，已有 30 cm/s 实车验收记录 | 左右独立 PI、前馈、斜坡、换向保护和错误锁存 |
| P5 七路循迹 | 已启用，已有稳定循迹实车记录 | 滤波 PD + 有方向记忆的全白找线 |
| P6.1 MPU6050 Yaw | 已启用，已有实车验收记录 | ±500 °/s、真实 elapsed 积分、Yaw 归一化 |
| P6.2 航向与转角 | 源码已启用，未找到完整验收矩阵记录 | 双轮反向转角和绝对 Yaw 航向保持均已接入 |
| P6.3 定距 | 2026-07-25 后源码已实现，未找到完整实车验收记录 | 已有纯定距及“定距 + 绝对 Yaw”，当前只支持正距离前进 |
| 新底层收口 | 已启用 | 旧 SEEK、单轮 90°、旧 LOST_RECOVER 被编译开关隔离 |
| HC-06 蓝牙 | 正在联调 | UART、RX 中断/轮询兜底、诊断回显、环形缓冲、OLED 诊断和 PC 工具已存在；命令协议尚未接入 |

这里的“编码器闭环电机控制”特指编码器反馈的双轮轮速闭环，不等同于位置闭环。定距动作是在轮速闭环之上的第二层距离状态机。

当前关键功能开关：

```c
APP_PROFILE                         = APP_PROFILE_DEVELOPMENT
FEATURE_HW_MOTOR_PWM                = 1
FEATURE_WHEEL_SPEED_ESTIMATOR       = 1
FEATURE_WHEEL_SPEED_CONTROL         = 1
FEATURE_MOTOR_CONTROL_FAST_SETTLING = 1
FEATURE_WHEEL_SPEED_TEST            = 0
FEATURE_LEGACY_MOTION_CONTROL       = 0
FEATURE_LINE_CONTROL_V2             = 1
ENABLE_IMU                          = 1
ENABLE_HEADING_CONTROL              = 1
FEATURE_BLUETOOTH_UART              = 1
FEATURE_GIMBAL_MOTION_CONTROL       = 0
FEATURE_GIMBAL_OLED_TEST            = 0
FEATURE_OBSTACLE_SCANNER            = 0
FEATURE_HARDWARE_WATCHDOG           = 0
```

## 2. 分层设计

### 2.1 主控制链

```text
MissionLibrary
  静态任务数组、动作参数、开发/比赛任务注册表
        ↓
MissionManager
  选择任务、推进 action_index、重试、暂停、外部冻结
        ↓
MotionAction
  把一个动作的开始/运行/完成/超时转换为统一语义
        ↓
CarController
  唯一正常运动仲裁点，决定当前 run_mode 和左右 normalized 目标
        ↓
LineController / HeadingControl / 定距状态机
  生成本周期左右命令
        ↓
MotorControl
  normalized → cm/s，执行左右轮 PI + 前馈
        ↓
Motor
  最终方向 GPIO、TIMG8 PWM 和强制停车
```

反馈链：

```text
编码器 GPIO ISR
→ Encoder 有符号 delta
→ WheelSpeedEstimator 唯一读清 delta
→ 左右轮速、左右累计距离、中心累计距离
→ MotorControl 轮速反馈
→ 定距动作的 travelled / remaining
```

### 2.2 安全链

```text
K2 长按软件急停
/ 软件 heartbeat watchdog
/ ObstacleSafety
/ 编码器估算器错误
/ MotorControl 错误或目标刷新超时
/ IMU 无效
/ 动作超时
→ 阻止普通目标
→ MotorControl_Stop / Motor_Stop
→ PWM 强制低
→ 必要时 Fault 锁存并让 Mission 进入 ERROR
```

### 2.3 必须保持的边界

- `MissionLibrary` 只描述“做什么”，不直接控制电机。
- `MotionAction` 管理动作语义，不直接调用 `Motor_SetSpeed()`。
- `CarController` 是唯一正常运动仲裁点；普通业务只能在这里提交左右 normalized 目标。
- 启用 P4 后，正常运动最终只由 `MotorControl` 调用 `Motor_SetSpeed()`。
- `Encoder_GetAndClearPulseDeltas()` 只能由 `WheelSpeedEstimator_Update()` 消费，不能让 OLED、定距动作或其他模块再次读清。
- 编码器 ISR 只做边沿判向、计数和溢出标记，不放浮点、滤波、任务逻辑或打印。
- `ObstacleSafety`、急停和 watchdog 的优先级高于所有普通动作。
- `Debug/ti_msp_dl_config.*` 是 SysConfig 生成物；硬件配置应修改 `empty.syscfg`，不要手改生成文件。

## 3. 工程目录与推荐阅读顺序

79 是 Git 工作区根目录；完整 CCS 工程及业务源码都位于 `diansaikaishi/`。

推荐按以下顺序理解当前主线：

```text
app_features.h
app_config.h / app_config.c
empty.syscfg
empty.c
app.c

motor.c
encoder.c
wheel_speed_estimator.c
motor_control.c

track_sensor.c
line_controller.c
imu.c
angle_utils.c
heading_control.c
car_controller.c

motion_types.h
motion_action.c
mission_manager.c
mission_library.c

emergency_stop.c
watchdog_monitor.c
fault.c
obstacle_monitor.c
obstacle_safety.c

menu.c
oled_ui.c
runtime_snapshot.c

bluetooth_uart.c
tools/bluetooth_serial_console.py
```

主要职责：

| 文件 | 当前职责 |
|---|---|
| `empty.c` | `main`、100 us SysTick、pending 调度、编码器 GPIO 中断入口 |
| `app.c` | 20 ms 主控制顺序和模块初始化 |
| `app_features.h` | 开发/比赛配置和功能门控 |
| `app_config.c/.h` | 控制参数默认值与合法范围 |
| `motor.c/.h` | 最终 PWM、方向、停车和软件 PWM 回退 |
| `encoder.c/.h` | ENCA 双边沿计数与原子读清 |
| `wheel_speed_estimator.c/.h` | 轮速滤波、累计脉冲和里程 |
| `motor_control.c/.h` | 双轮 PI + 前馈闭环 |
| `track_sensor.c/.h` | 七路数字循迹读取、加权误差和旧 90°事件识别 |
| `line_controller.c/.h` | P5 滤波 PD 和全白恢复 |
| `imu.c/.h` | MPU6050、gyro bias、Yaw 积分和有效性 |
| `heading_control.c/.h` | 绝对 Yaw 航向误差控制 |
| `car_controller.c/.h` | run_mode、普通运动仲裁和动作底层状态 |
| `motion_action.c/.h` | 单动作生命周期和结果映射 |
| `mission_manager.c/.h` | 多动作顺序推进和任务状态 |
| `mission_library.c/.h` | 当前测试任务、RACE-1 和动作宏 |
| `bluetooth_uart.c/.h` | HC-06 UART 字节收发基础层 |

视觉和云台仍有独立文件与文档：

- `Vision Target Protocol V1.md`
- `K230视觉-二维云台系统联调与参数调试手册 V1.0.md`

当前蓝牙占用 PB6/PB7，云台运动控制已通过功能开关关闭；视觉 UART 接收链仍保留。

## 4. 初始化与调度

### 4.1 启动顺序

```text
SYSCFG_DL_init()
→ App_Init()
→ BluetoothUart_Init()               FEATURE_BLUETOOTH_UART=1
→ VisionReceiver / VisionTuning / VisionUart 初始化
→ SysTick_Config(10 kHz)
→ 使能编码器 GPIO 中断
→ 进入主循环
```

`App_Init()` 内的关键顺序：

```text
Fault / EmergencyStop / Watchdog / RuntimeSnapshot
→ Motor_Init，并先保持输出为低
→ Encoder_Reset
→ AppConfig_InitDefault
→ WheelSpeedEstimator_Init
→ MotorControl_Init
→ IMU 初始化、200 次 bias 校准、Yaw 清零
→ HeadingControl / CarState / Menu / Mission
→ Track / Ultrasonic / Obstacle / Servo / CarController / Key / OLED
→ Motor_Stop
```

车辆上电时应保持静止，让 IMU 完成 gyro bias 校准。

### 4.2 100 us SysTick

当前 SysTick 为 10 kHz：

```text
每 100 us：
  Servo_Tick100us()
  Ultrasonic_Tick100us()

每 1 ms：
  系统时间 +1
  软件 heartbeat watchdog ISR 检查

每 20 ms：
  只增加 App pending，不在 ISR 内执行完整控制
```

由于 `FEATURE_HW_MOTOR_PWM=1`，`Motor_PwmTick100us()` 不会执行；电机 PWM 由 TIMG8 硬件持续产生。

云台 5 ms / 10 ms pending 链在 `FEATURE_GIMBAL_MOTION_CONTROL=0` 时不编译进当前运行路径。

### 4.3 主循环和真实 elapsed

主循环会合并积压的 20 ms pending，只执行一次 `App_Update_20ms(elapsed_ms)`，并记录 missed、drop、overrun 和 dt clamp。控制 elapsed 最大限制为 100 ms。

当前 `App_Update_20ms()` 顺序：

```text
1.  WheelSpeedEstimator_Update(elapsed_ms)
2.  Watchdog 故障应用
3.  Imu_Update(elapsed_ms)
4.  Ultrasonic_Update_20ms()
5.  ObstacleMonitor_Update_20ms()
6.  舵机保持前向 SV 角度
7.  Key_Update_20ms() + Menu_HandleKeyEvent()
8.  再检查急停和 watchdog；命中则立即停车并提前返回
9.  MissionManager_Update_20ms(elapsed_ms)
10. ObstacleSafety_Update_20ms()
11. ObstacleAvoidance_Update_20ms(elapsed_ms)
12. CarController_Update_20ms(elapsed_ms)
13. MotorControl_Update(elapsed_ms)
14. RuntimeSnapshot / DebugTelemetry / OLED
```

顺序上的关键点：

- 先更新编码器估算，再运行本周期控制器。
- `CarController` 先提交目标，`MotorControl` 后执行轮速闭环。
- 安全链位于最终输出之前。
- OLED 在控制完成后读取状态，不拥有控制权。

视觉 UART 的字节处理在主循环中持续执行，不属于 20 ms 小车动作链。

## 5. 新底层：PWM、编码器和双轮轮速闭环

### 5.1 命令单位

上层统一使用有符号 normalized 命令：

```text
-1000 ～ +1000
```

启用 P4 后的映射：

```text
target_speed_cmps = normalized_command / 1000 × 60
```

例如：

| normalized | 目标轮速 |
|---:|---:|
| 100 | 6 cm/s |
| 200 | 12 cm/s |
| 220 | 13.2 cm/s |
| 280 | 16.8 cm/s |
| 1000 | 60 cm/s |

### 5.2 最终硬件输出

`Motor_SetSpeed()` 负责：

- 将命令限幅到 `-1000～1000`；
- 根据正负号设置 TB6612 方向 GPIO；
- 换向前先强制相应 PWM 为低；
- 将绝对值映射为 TIMG8 compare；
- 零命令时强制 PWM 低；
- 保存 requested、applied、compare 和方向诊断。

当前 PWM：

```text
TIMG8 / BUSCLK 32 MHz
timerCount = 1600
频率 = 32 MHz / 1600 = 20 kHz
MOTOR_A_PWM = PB15
MOTOR_B_PWM = PB16
```

`Motor_Stop()` 在临界区内同时强制两路 PWM 为低、清方向 GPIO 和运行时命令。软件 PWM 代码仍在，但只在 `FEATURE_HW_MOTOR_PWM=0` 时编译使用。

### 5.3 编码器解码与里程

当前硬件：

| 信号 | 引脚 | 行为 |
|---|---|---|
| MOTOR_A_ENCA | PA15 | 上升沿和下降沿中断 |
| MOTOR_A_ENCB | PA16 | ISR 内读取方向 |
| MOTOR_B_ENCA | PA17 | 上升沿和下降沿中断 |
| MOTOR_B_ENCB | PA24 | ISR 内读取方向 |

ISR 在 ENCA 每个边沿读取 ENCA/ENCB 相位关系，因此属于 ENCA 双边沿 ×2 解码。

当前标定：

```text
encoder_ppr_x2         = 734 pulse/rev
wheel_diameter_cm      = 6.5 cm
wheel_track_cm         = 11.25 cm
left_encoder_direction = -1
right_encoder_direction= +1
```

计算关系：

```text
distance_per_pulse = π × wheel_diameter_cm / encoder_ppr_x2
raw_speed_cmps = delta × distance_per_pulse × 1000 / elapsed_ms
wheel_distance = total_pulse × distance_per_pulse
center_distance = (left_distance + right_distance) / 2
```

轮速使用 `alpha=0.35` 的一阶 IIR：

```text
filtered = previous + 0.35 × (raw - previous)
```

估算器接受 `0 < elapsed ≤ 100 ms`，但 `elapsed > 60 ms` 会标为 stale；配置错误、计数溢出和非法 dt 都会进入 `error_flags`。累计脉冲和距离不会在每次动作开始时清零，定距动作只保存一个中心里程基准。

### 5.4 双轮轮速闭环

左右轮独立执行：

```text
error = ramped_target_speed - measured_filtered_speed
feedforward = ramped_target / 60 × 1000 × feedforward_gain
output = feedforward + kp_used × error + ki × integral
```

当前参数：

| 参数 | 左轮 | 右轮 |
|---|---:|---:|
| 最大映射速度 | 60 cm/s | 60 cm/s |
| 加速侧 Kp | 4.0 | 4.0 |
| 超速侧 Kp | 5.0 | 5.0 |
| Ki | 1.0 | 1.0 |
| 前馈系数 | 0.5 | 0.5 |
| 积分限幅 | 300 | 300 |
| 积分释放倍率 | 4 | 4 |
| 最大加速度 | 60 cm/s² | 60 cm/s² |
| 最大减速度 | 120 cm/s² | 120 cm/s² |
| 目标刷新超时 | 100 ms | 100 ms |

闭环还包含：

- 输出限幅和 anti-windup；
- 超速侧使用更高 Kp；
- 误差换向时快速释放积分；
- 目标斜坡与安全换向；
- 零目标直接停车；
- 估算器无效、stale、错误或反馈超范围时锁存停车；
- 非 `CAR_STATE_RUNNING` 时停车；
- 急停、watchdog 和 SafetyHold 抑制输出；
- 上层超过 100 ms 不刷新非零目标时故障停车。

发生控制错误后，普通 Stop 不会自动清除 `error_latched`；应通过 K3 长按执行完整复位。

## 6. P5 七路循迹

### 6.1 传感器和误差

七路数字量从左到右为 S1～S7，黑线有效，权重为：

```text
S1   S2   S3   S4   S5   S6   S7
-300 -200 -100   0  +100 +200 +300
```

多个传感器同时有效时取有效权重的平均值。

### 6.2 当前控制公式

```text
raw_error
→ error IIR，alpha=0.35
→ 使用真实 elapsed 计算微分
→ derivative IIR，alpha=0.20
→ correction = 0.35 × filtered_error
             + 0.010 × filtered_derivative
→ correction 限幅 ±120
→ left  = 220 + correction
→ right = 220 - correction
```

普通 FOLLOW 不允许反转。某侧命令落在 `1～49` 时会直接归零，避免低于稳定运行区间。

### 6.3 全白恢复

`LineController` 在仍看到线时记录边缘方向：

```text
S1 有效且 S7 无效 → 记为 LEFT
S7 有效且 S1 无效 → 记为 RIGHT
```

进入全白后：

```text
记为 LEFT  → left=0,   right=280
记为 RIGHT → left=280, right=0
重新看到任意黑线 → 当周期恢复 FOLLOW
持续全白 2000 ms → STOP / RECOVER_TIMEOUT
全白但此前没有方向 → STOP / NO_DIRECTION
```

P5 的丢线恢复位于 `LineController` 内，不依赖已关闭的旧 `TRACK_MODE_LOST_RECOVER`。

### 6.4 当前调参边界

真正控制 P5 的参数是 `line_control_v2_*` 字段。当前 OLED 参数页的 `SPD/KP/KD/MAX` 修改的是旧 `base_speed/track_kp/track_kd/max_correction`，在 `FEATURE_LINE_CONTROL_V2=1` 且旧底层关闭时不会改变当前 P5 循迹参数。

因此不要把 OLED 上旧参数值的变化误认为已经在线修改 P5。若以后需要现场调 P5，应新增明确的 V2 菜单项，不能继续复用含义不同的旧字段。

## 7. IMU、航向、转角和定距

### 7.1 IMU 与 HeadingControl

当前 MPU6050 关键参数：

```text
gyro 量程        ±500 °/s
灵敏度           65.5 LSB/(°/s)
Yaw 轴符号       -1
gyro 死区        1.5 °/s
有效 dt          5～100 ms
短缺帧补偿上限   60 ms
stale 阈值       100 ms
Yaw 范围         [-180°, 180°)
```

`HeadingControl` 使用绝对 Yaw 目标：

```text
error = Normalize180(target_yaw - current_yaw)
derivative = -gyro_z_dps
correction = clamp((30 × error + 5 × derivative) / 10, -30, 30)
```

注意：当前源码 `heading_kp=30`，已不同于 2026-07-24 P6 方案中记录的 20。

### 7.2 当前允许的动作

新底层正式允许：

```text
FOLLOW_LINE
TURN_TO_YAW
DRIVE_HEADING_UNTIL_LINE
DRIVE_DISTANCE
DRIVE_DISTANCE_HEADING
WAIT
STOP
```

以下旧动作类型仍留在枚举或源码中，但在 `FEATURE_LEGACY_MOTION_CONTROL=0` 时不会成为合法任务动作或获得控制权：

```text
SEEK_LINE
TURN_LEFT_90
TURN_RIGHT_90
DRIVE_HEADING_TIME
旧 LOST_RECOVER
```

### 7.3 相对 Yaw 转角

启动时：

```text
target_yaw = Normalize180(current_yaw + relative_angle)
```

运行逻辑：

```text
|error| > 12°      → 双轮反向，命令 130
5° < |error| ≤ 12° → 双轮反向，命令 65
|error| ≤ 5°       → 0/0，检查 |gyro| ≤ 5 °/s
连续稳定 100 ms    → SUCCESS
达到 timeout       → ERROR 并持续停车
```

当前没有独立 FAST/SLOW/SETTLE 枚举，也没有 7°退出迟滞；这是 P6 文档的建议状态机，不是当前源码事实。

### 7.4 绝对航向找线

`DRIVE_HEADING_UNTIL_LINE(target_yaw, command, timeout)` 用于从无黑线区域沿绝对 Yaw 前进，任意一个循迹传感器看到黑线后动作成功。

当前 RACE-1 使用：

```text
target_yaw = 0°
command = 200
timeout = 5000 ms
```

该 0° 是上电 IMU 校准并清零后的绝对 Yaw，不是进入动作时自动锁存的当前方向。

### 7.5 定距

当前有两个动作：

```text
DRIVE_DISTANCE(distance_cm, command)
DRIVE_DISTANCE_HEADING(distance_cm, absolute_target_yaw, command)
```

共同逻辑：

```text
start = center_distance_cm
travelled = current_center_distance - start
remaining = target_distance - travelled

remaining > 5.0 cm  → 正常命令
0.8 cm < remaining ≤ 5.0 cm → 命令不高于 130
remaining ≤ 0.8 cm → 0/0，进入 SETTLE
左右轮速都 ≤ 2.0 cm/s，持续 200 ms → DONE
```

`DRIVE_DISTANCE_HEADING` 还会：

- 使用指定的绝对 Yaw，而不是自动锁存当前 Yaw；
- 要求动作开始时与目标 Yaw 的差不超过 20°；
- 使用 HeadingControl 修正左右命令；
- 动态限幅 correction，使两轮保持非负且不超过 1000，不在定距直行中反转。

当前实现边界：

- 任务校验只允许 `distance_cm > 0`，尚不支持倒车定距；
- 没有越界后反向修正；越过终点会直接进入停车/停稳判断；
- P6.3 已有源码与 OLED 诊断，但未找到 20/50/100 cm、不同速度、扰动、异常等完整实车验收记录；
- Pause 会 `MotionAction_Init()`，Resume 后会从当前位置重新开始当前动作的完整目标距离，不会继续剩余距离；定距任务调试期间不要使用 Pause/Resume。

## 8. 当前任务库

当前为开发配置，OLED 可见四个任务：

| 序号 | 名称 | 动作 |
|---:|---|---|
| 1 | TEST-LINE | WAIT 500 ms → 从当前可见线开始持续 P5 FOLLOW，障碍策略 STOP_ONLY |
| 2 | TEST-TURN | WAIT 500 ms → 相对 +90°，timeout 4000 ms → STOP |
| 3 | TEST-DIST | WAIT 500 ms → 前进 20 cm，同时保持绝对 Yaw 45°，command 200，timeout 5000 ms → STOP |
| 4 | RACE-1 | 沿绝对 Yaw 0°、command 200 找到任意黑线 → 持续 P5 FOLLOW，STOP_ONLY |

TEST-DIST 启动时的实际 Yaw 必须位于目标 45°的 ±20°范围内，否则会得到 `HEADING_START_MISMATCH`。上电 Yaw 为 0°时不能直接原姿态运行这个测试，应先让车体实际朝向约 45°，或为本轮测试修改目标 Yaw。

切换到 `APP_PROFILE_COMPETITION` 后，只注册 RACE-1。

RACE-1 只是当前第一版新底层集成任务，不等于正式比赛路线已经编排完成。

### 8.1 新增任务的正确位置

地图和流程变化优先只修改：

```text
mission_library.h
mission_library.c
```

步骤：

1. 定义稳定的 mission ID；开发测试建议使用 100 以上，正式任务使用 10～99。
2. 在 `mission_library.c` 新建静态 `MotionAction` 数组。
3. 使用当前允许的新动作宏拼接流程。
4. 最后必须有 `ACTION_STOP()`。
5. 有结束条件的正式动作必须设置非零 timeout。
6. 在 `g_missionRegistry[]` 的正确 profile 分支注册。
7. 先在 Development 单动作验证，再进入 Competition。

不要为了新增地图任务去修改 `MotorControl`、`LineController` 或编码器 ISR。

## 9. 安全、障碍与故障

### 9.1 软件急停和复位

- 任意普通页面 K2 长按：触发软件急停，立即停车。
- 急停状态下 K3 长按：解除急停并回 HOME。
- 普通状态 K3 长按：清 Fault、重置 watchdog、任务、CarController、MotorControl 和避障状态，保持停车等待重新启动。

### 9.2 Watchdog

当前只有软件 heartbeat watchdog。它可在 SysTick 仍运行、主控制长期没有完成时从 ISR 路径强制 `Motor_Stop()`。

`FEATURE_HARDWARE_WATCHDOG=0`，`empty.syscfg` 没有已审核的硬件 WWDT 实例，因此不能把当前工程描述为具有硬件 watchdog 复位覆盖。

### 9.3 超声波和 STOP_ONLY

HC-SR04 当前阈值：

```text
距离 < 20 cm，连续 3 次 → BLOCKED
距离 ≥ 25 cm，连续 3 次 → CLEAR
20～25 cm → 保持原状态
```

当前新底层所有可见 FOLLOW 任务使用 `OBSTACLE_POLICY_STOP_ONLY`：

```text
BLOCKED
→ CarController SafetyHold
→ MissionManager ExternalHold
→ 电机停车，任务时间冻结
→ 障碍清除后解除 Hold，继续当前动作
```

### 9.4 固定主动绕障的当前状态

旧固定绕障状态机源码仍在 `obstacle_avoidance.c`，但主体被 `FEATURE_LEGACY_MOTION_CONTROL` 包围。当前开关为 0，因此：

- 固定主动绕障运行逻辑不执行；
- MissionLibrary 会拒绝新底层任务中的 `OBSTACLE_POLICY_FIXED_BYPASS`；
- 当前真正生效的只有 STOP_ONLY 安全停车；
- 舵机扫描器也处于关闭状态。

旧《工程文档》中“+35° → 航向直行 1400 ms → -55° → 回线”的描述只属于保留的旧链路，不是当前新底层主线。

## 10. HC-06 蓝牙联调

### 10.1 当前已经实现

```text
HC-06 Bluetooth Classic SPP
↕ 9600 8N1，无流控
MSPM0 UART1
↕ RX 中断 + 主循环轮询兜底 / TX
bluetooth_uart.c
↕
128 字节 RX 环形缓冲 + 临时逐字节回显 + Runtime 诊断
↕
OLED BT 页面 / 预留字节读写 API
```

硬件连接按 MCU 方向命名：

| MCU | HC-06 | 说明 |
|---|---|---|
| PB6 UART TX | 模块 RXD | MCU 发、模块收 |
| PB7 UART RX | 模块 TXD | 模块发、MCU 收 |
| GND | GND | 必须共地 |

配置：

```text
UART_BLUETOOTH = UART1
baud = 9600
data = 8 bit
parity = none
stop = 1 bit
flow control = none
RX interrupt = enabled
FIFO = disabled
```

固件按 HC-06 常见出厂透明串口参数 9600 8N1 工作，不会在小车启动时自动
进入 AT 模式或反复修改模块配置。HC-06 的 AT 行为在不同克隆固件间有差异，
且通常只在未建立蓝牙连接时有效；若新模块曾被改过波特率，应先用 USB-TTL
在断开蓝牙连接的状态下恢复/确认 9600，再接入小车。

`BluetoothUart_Init()` 会清空 RX head/tail、清计数并使能 UART1 IRQ。RX ISR 会：

- 读取所有可用字节；
- 更新 `irq_count`、`rx_byte_count` 和 `last_rx_byte`；
- 将字节写入 128 大小的环形缓冲；
- 在 `FEATURE_BLUETOOTH_RX_ECHO=1` 时立即把字节回显给电脑；
- 缓冲满时保留旧数据、丢弃新字节并增加 `rx_overflow_count`。

主循环持续调用 `BluetoothUart_Process()`：它读取 PB6/PB7 的 PINCM 复用状态
和 PB7 当前电平；若 UART 硬件中已经有数据但 RX 中断未处理，则以轮询方式取出并增加
`polled_rx_byte_count`。正常情况下字节应走中断，`P` 保持 0；若 `RX`
增长且 `P` 同步增长，则说明字节是由轮询兜底救回，应该继续排查 NVIC/IRQ。

环形缓冲用一个空槽区分满/空，实际最多保存 127 个尚未消费的字节。

### 10.2 当前尚未实现

- 没有应用层蓝牙帧格式或命令协议；
- 没有把蓝牙输入映射为任务选择、启动、停止或运动命令；
- 当前没有业务代码调用 `BluetoothUart_TryReadByte()`，所以收到的数据会留在环形缓冲，持续发送最终会 overflow；
- 当前 TX 只用于联调期逐字节回显，不代表应用层命令应答协议已经完成；
- 蓝牙未接入 RuntimeSnapshot、MissionManager 或 CarController。

因此当前蓝牙状态应准确写成“UART 收发基础层与 RX 可视化联调中”，不能写成“蓝牙遥控已完成”。

### 10.3 OLED 蓝牙页

紧凑页面循环中有 BT 页，显示：

```text
I    初始化标志
M    PB6/PB7 当前 PINCM 是否仍为 UART1 TX/RX；1 表示未被云台 GPIO 覆盖
H    PB7 当前电平；UART 空闲时通常为 1
RX   ISR 或轮询已收到的累计字节数
L    最后收到字节的十进制值
IRQ  UART1 中断处理函数进入次数
P    被主循环轮询兜底取出的字节数
T    成功回显/发送的累计字节数
O    RX 环形缓冲溢出次数
D    回显时 TX 忙导致的丢弃次数
```

发送 ASCII `A` 时，正确现象应为：

```text
RX 增加 1
L 显示 65
M 保持 1，H 空闲时为 1
IRQ 增加，P 保持 0
T 增加 1，O/D 保持 0
```

PC 工具还应收到回显 `[RX 1 B] HEX=41 TEXT='A'`。若 `RX` 增加但电脑没有
回显，查看 `T` 和 `D`；若 `RX` 不变但 `P` 增加，说明接收中断存在问题。

### 10.4 PC 调试工具

工具：

```text
tools/bluetooth_serial_console.py
```

依赖：

```powershell
pip install pyserial
```

常用命令：

```powershell
python tools/bluetooth_serial_console.py --list
python tools/bluetooth_serial_console.py --port COMx
python tools/bluetooth_serial_console.py --port COMx --hex "41"
```

更换模块后必须先在 Windows 删除/忽略旧模块的 COM15，重新配对 HC-06，
再用 `--list` 确认新生成的出站 COM 口。工具不再内置旧模块蓝牙地址；若
Windows 串口描述中没有显示 `HC-06`，必须显式传入 `--port COMx`，也可以
用 `--address` 传入新模块蓝牙地址辅助自动识别。

交互模式内：

```text
test              发送 ASCII A
send <text>       原样发送文本
line <text>       文本后附加 LF
hex AA 55 01      发送十六进制字节
ports             列出串口
quit              退出
```

工具刻意不把普通键盘输入直接变成运动命令，为后续协议尚未完成的阶段保留安全边界。

### 10.5 蓝牙与云台的资源冲突

PB6/PB7 已分配给 HC-06，因此当前：

```c
FEATURE_BLUETOOTH_UART = 1
FEATURE_GIMBAL_MOTION_CONTROL = 0
```

若同时启用两者，`app_features.h` 会编译报错。以后恢复云台运动前必须先重新分配引脚并更新 `empty.syscfg`，不能只改功能宏。

## 11. OLED 与按键

### 11.1 当前紧凑页面循环

`FEATURE_OLED_LEGACY_DIAG_PAGES=0` 且蓝牙开启时：

```text
STATUS → SENSOR → HEADING → DISTANCE → BLUETOOTH → STATUS
```

源码注释也将其简称为：

```text
HOME → LINE → TURN → DIST → BT
```

旧 IMU 明细、编码器、MotorControl 和障碍页面代码仍保留；将 legacy diag 开关改为 1 后才进入更长的诊断循环。

OLED 每 100 ms 重建缓冲，并分行刷新显示，避免一次刷新占用过长。

### 11.2 普通页面按键

```text
K1 短按  切换页面
K1 长按  进入参数页
K2 短按  READY→启动，RUNNING→暂停，PAUSED→恢复
K2 长按  软件急停
K3 短按  取消任务并回 READY
K3 长按  清故障并完整复位底层和任务
```

参数页：

```text
K1 短按  下一参数
K1 长按  返回进入参数页前的页面
K2 短/长  增加/快速增加
K3 短/长  减少/快速减少
```

当前参数项：

```text
TASK / SPD / KP / KD / MAX / SV / WLK
```

其中 SPD、KP、KD、MAX 是旧控制参数，当前不会调节 P5 V2；WLK 在云台运动关闭时固定返回 0。

## 12. 当前硬件资源摘要

| 功能 | 资源/引脚 |
|---|---|
| 电机 PWM A/B | TIMG8 CCP0 PB15 / CCP1 PB16，20 kHz |
| TB6612 A 方向 | PA13 AIN1 / PA12 AIN2 |
| TB6612 B 方向 | PB0 BIN1 / PB1 BIN2 |
| 编码器 A | PA15 ENCA / PA16 ENCB |
| 编码器 B | PA17 ENCA / PA24 ENCB |
| 循迹 S1/S2/S3 | PB25 / PB24 / PB20 |
| 循迹 S4 | PA14 |
| 循迹 S5/S6/S7 | PB18 / PB19 / PB10 |
| IMU 软件 I2C | PA10 SDA / PA11 SCL |
| HC-SR04 | PA8 TRIG / PA9 ECHO |
| OLED | PA28 SDA / PA31 SCL |
| 按键 K1/K2/K3 | PA26 / PA25 / PA27 |
| 舵机 | PB9 |
| 视觉 UART | PB2 TX / PB3 RX，115200 |
| 蓝牙 UART | PB6 TX / PB7 RX，9600 |

HC-SR04 ECHO 常见为 5 V，必须确认扩展板已分压或做电平转换后再接 MCU。

## 13. P6 方案之后已经发生的变化

`P6收尾与新版底层全面迁移总体方案.md` 审计的是 2026-07-24 状态。此后源码已经发生以下关键变化：

1. 新增了 `DRIVE_DISTANCE` 和 `DRIVE_DISTANCE_HEADING` 实现及 OLED 诊断；
2. 新增了 `DRIVE_HEADING_UNTIL_LINE`；
3. `FEATURE_LEGACY_MOTION_CONTROL=0`，旧 SEEK、单轮 90°、旧丢线恢复和旧按时航向动作被隔离；
4. 旧 MAP-A/B/C 被当前 `RACE-1` 取代；
5. 新底层任务只允许 STOP_ONLY，旧固定主动绕障被编译关闭；
6. 当前 HeadingControl Kp 从 P6 文档记录的 20 变为 30；
7. OLED 紧凑循环变为 HOME/LINE/TURN/DIST/BT；
8. 新增蓝牙 UART，PB6/PB7 改作蓝牙，云台运动控制关闭；联调模块随后由 JDY-31 更换为 HC-06。

因此，P6 文档仍适合查看分层原则、验收纪律和历史决策，但其中“P6.3 尚未实现”“MAP-A/B 仍用旧 90°”“当前无蓝牙”等旧状态不能直接沿用。

## 14. 当前能力边界与下一步

已经形成的主线能力：

```text
硬件 PWM
→ 编码器轮速/里程
→ 双轮轮速闭环
→ 七路滤波 PD 循迹
→ 全白有界恢复
→ MPU6050 Yaw
→ 相对角度转向
→ 绝对航向找线
→ 前进定距
→ 前进定距并保持绝对 Yaw
→ 任务数组编排
→ 急停、软件 watchdog、障碍 STOP_ONLY 和故障锁存
```

尚未完成或缺少完整证据：

```text
蓝牙应用层协议、字节消费和 MCU 主动发送
蓝牙对任务层的安全控制接口
P6.2 全角度、扰动、超时和 DONE 保持完整实车验收
P6.3 20/50/100 cm、不同速度、异常和重复动作完整实车验收
倒车定距
定距 Pause/Resume 的剩余距离语义
正式比赛路线编排和 Competition 全流程回归
新底层固定主动绕障
硬件 WWDT
参数 Flash 持久化
```

当前推荐顺序：

```text
1. 先完成蓝牙 RX 物理链路验证：I=1、RX 增长、L 正确、OV=0
2. 再定义最小且安全的蓝牙应用协议；协议完成前不接运动控制
3. 独立补齐 TURN_TO_YAW 和航向保持实车验收
4. 独立补齐定距与定距航向实车验收
5. 用已验收动作编排正式 RACE 任务
6. 切 Competition，Clean/Rebuild、完整擦除烧录并做全流程回归
```

## 15. 不要轻易修改的点

```text
不要让 Mission、蓝牙协议或 ObstacleAvoidance 直接调用 Motor_SetSpeed
不要创建第二个编码器 delta 消费者
不要用清累计里程的方式开始定距动作
不要通过修改 P3 比例或 P4 PI 掩盖定距状态机问题
不要通过额外取负或经验比例掩盖 Yaw/Heading 符号问题
不要在 FEATURE_LEGACY_MOTION_CONTROL=0 时误以为旧绕障仍生效
不要把 OLED 的旧 SPD/KP/KD/MAX 当成 P5 V2 在线参数
不要在定距动作中使用当前 Pause/Resume 语义
不要同时启用蓝牙和云台运动而不先解决 PB6/PB7 资源冲突
不要手改 Debug/ 下的 SysConfig 生成文件或编译产物
不要把软件 heartbeat watchdog 写成硬件 WWDT
```

完成关键功能修改后，固定执行：

```text
CCS Clean Project
→ Rebuild Project
→ 确认关键源文件重新编译
→ 核对 .out 时间戳和烧录路径
→ 完整擦除并烧录最新镜像
→ 上电静止完成 IMU 校准
→ 单动作验证
→ 组合任务回归
```
